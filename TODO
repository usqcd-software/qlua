Ticket #14: aff:building_blocks(Cf, p*, prefix, multiset)
              p[i] = {L:complex phase, p[0], p[1], ..., p[#L-1]}

-- QDP/C QIO: writing sections of the fields -- extend the interface.

MDWF
  operator
  conjugate operator
  solvers

Documentation
+  Examples in the distribution tree  -- started
+  User guide - started in wiki
  Programming guide
