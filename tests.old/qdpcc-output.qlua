L = qcd.lattice{4,4,4,8}

do
  local x = L:Int(0)
  for i = 0, qcd.Nd - 1 do
    x = x * L[i] + L:pcoord(i)
  end
  S = L:RandomState(1235, x)
end

F = S:gaussian_DiracPropagator()

qcd.qdpcc.write_prop("D", "test-qdpcc.P", "file info", F, "record info")

io.stdout:write("------ done\n")
