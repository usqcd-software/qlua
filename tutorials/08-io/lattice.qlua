-- setup the lattice and initialize the random state
L = qcd.lattice{4,6,8}

do
  local x = L:Int(0)
  for i = 0, #L - 1 do
    x = x * L[i] + L:pcoord(i)
  end
  S = L:RandomState(os.random(), L:Int(os.random()) + x)
end
